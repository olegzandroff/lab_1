#include "libraries.h"
#include "prototypes.h"

bool sem_4_bad_scenario_1(const matrix_elem_t* const* array, int n, int m) {
	//bool flag = false;
	//for (int i = 0; i < n; i++)
	//{
	//	int counter = 0;
	//	for (int j = 0; j < m; j++){
	//		if (array[i][j] % 2 == 0 && array[i][j] > 0)
	//			counter++;
	//	if (counter>=2) counter
	//}
	return false;
}

bool sem_4_bad_scenario_2(const matrix_elem_t* const* array, int n, int m) {
	return false;
}

bool sem_4_bad_scenario_3(const matrix_elem_t* const* array, int n, int m) {
	return false;
}
